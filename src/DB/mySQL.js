const mysql = require('mysql');
const config = require('../config');

function getAll(table){

}

function getRegister(table, id) {
    
}

function addRegister(table, id) {
    
}

function removeRegister(table, id) {
    
}

module.exports = {
    getAll,
    getRegister,
    addRegister,
    removeRegister,
}